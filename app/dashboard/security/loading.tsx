import { ModulePageSkeleton } from "@/components/ui/loading-states";

export default function DashboardSecurityLoading() {
  return <ModulePageSkeleton titleWidth="w-64" subtitleWidth="w-80" rows={6} cols={5} />;
}
