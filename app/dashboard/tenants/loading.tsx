import { ModulePageSkeleton } from "@/components/ui/loading-states";

export default function DashboardTenantsLoading() {
  return <ModulePageSkeleton titleWidth="w-60" subtitleWidth="w-80" rows={7} cols={6} />;
}
